apiVersion: v1
kind: ConfigMap
metadata:
  name: peakees-config
  namespace: peakees-production
  labels:
    app: peakees
data:
  NODE_ENV: "production"
  NEXT_TELEMETRY_DISABLED: "1"
  PORT: "3000"
  HOSTNAME: "0.0.0.0"
  # Add other non-sensitive configuration here